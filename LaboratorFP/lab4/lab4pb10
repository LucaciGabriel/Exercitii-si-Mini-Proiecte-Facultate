
#include <stdio.h>
#include <math.h>

int main()
{
    int i,max=-9999,a[5];
    for(i=1;i<=5;i++)
    {
        scanf("%d",&a[i]);
        if(max<a[i])max=a[i];
    }
    printf("Maximul este %d",max);
    return 0;
}
