
#include <stdio.h>
#include <math.h>

int main()
{
    int n,i;
    scanf("%d",&n);
    for(i=1000;i<=9999;i++)
    {
        if((i%n)==0) printf("%d este divizibil cu %d\n",i,n);
    }
    return 0;
    
}
